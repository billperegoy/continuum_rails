%h1 Release List for Project #{@project.name}

%ul
  - @releases.each do |release|
    %li 
      %p ID: #{release.git_id}
      %p MSG: #{release.commit_message}
      %p TIMESTAMP: #{release.commit_timestamp}
      %p NAME: #{release.committer_name}

